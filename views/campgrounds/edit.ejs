<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Campground</title>
</head>
<body>
    <h1>Edit Campground</h1>
    <form action='/campgrounds/<%= campground.id %>?_method=PUT' method="POST">
        <div>
          <label for="title">Title</label>
          <input  id="title" type="text" placeholder="title" name="campground[title]"
           value="<%= campground.title %>" />
       </div>
       <div>
          <label for="location">Location</label>
          <input  id="location" type="text" placeholder="location" name="campground[location]" 
          value="<%= campground.location%>" />
       </div>
       <button>Update Campground</button>
      </form>
      <a href='/campgrounds'>Back To Campground</a>
</body>
</html>